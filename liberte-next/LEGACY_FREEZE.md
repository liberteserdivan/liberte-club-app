# Legacy freeze

- Dokunma: `src/**`, `api/**` (istisna: `api/n-auth.js`, `api/n-qr.js`, `api/n-cashier.js`), kök `capacitor.config.json` webDir
- Yeni özellik / rewrite: yalnızca `liberte-next/**`
- Acil prod hotfix: legacy'ye minimal patch + bu dosyaya not
