# Android P0 smoke — GitHub Actions emulator (BrowserStack yerine)
#
# Gerekli GitHub Secrets:
#   MOBILE_TEST_PHONE — test kullanici telefonu
#   MOBILE_TEST_PIN   — test kullanici PIN
#
# Manuel: Actions -> Android Mobile Smoke (Emulator) -> Run workflow

name: Android Mobile Smoke (Emulator)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'android/**'
      - 'api/**'
      - 'e2e/mobile/**'
      - 'scripts/run-local-android-smoke.mjs'
      - 'scripts/verify-local-mobile-test-env.mjs'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/android-mobile-smoke-emulator.yml'

concurrency:
  group: android-mobile-smoke-emulator
  cancel-in-progress: true

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: npm install
        run: npm ci --legacy-peer-deps

      - name: Web build
        env:
          VITE_API_BASE_URL: https://liberte-club-app.vercel.app
        run: npm run build:release

      - name: Capacitor Android sync
        run: |
          node scripts/generate-android-icons.mjs
          npx cap sync android

      - name: Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug
      - name: Test secret kontrolu
        env:
          MOBILE_TEST_PHONE: ${{ secrets.MOBILE_TEST_PHONE }}
          MOBILE_TEST_PIN: ${{ secrets.MOBILE_TEST_PIN }}
        run: node scripts/verify-local-mobile-test-env.mjs


      - name: KVM izinleri
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Emulatorde P0 smoke
        uses: reactivecircus/android-emulator-runner@v2
        env:
          MOBILE_TEST_PHONE: ${{ secrets.MOBILE_TEST_PHONE }}
          MOBILE_TEST_PIN: ${{ secrets.MOBILE_TEST_PIN }}
          MOBILE_SMOKE_P0_ONLY: 'true'
          MOBILE_ANDROID_APK_PATH: ${{ github.workspace }}/android/app/build/outputs/apk/debug/app-debug.apk
          VITE_API_BASE_URL: https://liberte-club-app.vercel.app
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          profile: pixel_6
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          disable-animations: true
          script: |
            bash <<'SMOKE_SCRIPT'
            set -euo pipefail
            set -x
            npm install --no-save appium@2.11.5 appium-uiautomator2-driver@3.9.0
            npx appium driver install --source=npm appium-uiautomator2-driver
            npx appium --address 127.0.0.1 --port 4723 --log appium.log --log-level info &
            APPIUM_PID=$!
            trap 'kill "$APPIUM_PID" 2>/dev/null || true' EXIT
            for i in $(seq 1 90); do
              if curl -sf http://127.0.0.1:4723/status >/dev/null; then break; fi
              sleep 2
            done
            curl -sf http://127.0.0.1:4723/status
            adb wait-for-device shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done'
            mkdir -p ci-logs
            node scripts/verify-local-mobile-test-env.mjs
            node scripts/run-local-android-smoke.mjs || {
              echo '--- appium.log tail ---'
              tail -n 120 appium.log || true
              cp appium.log ci-logs/ 2>/dev/null || true
              cp e2e/mobile/reports/*.log ci-logs/ 2>/dev/null || true
              exit 1
            }
            SMOKE_SCRIPT

      - name: Rapor artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mobile-smoke-emulator
          path: |
            e2e/mobile/reports/*.json
            e2e/mobile/reports/*.log
            ci-logs/
            appium.log
          if-no-files-found: ignore